import Bookings from "./Bookings"

class User {
  constructor(id, name) {
    this.id = id;
    this.name = name;
  }

  // getUserBookings(bookings) {
  //   console.log(currentUser)
  // }

}
export default User;
