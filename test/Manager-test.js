import chai from 'chai';
const expect = chai.expect;

import Manager from "../src/Manager"

describe('Manager Class', function() {
  it('be able to delete booking', function() {
    expect(true).to.equal(true);
  });
});
